apiVersion: batch/v1
kind: CronJob
metadata:
  name: k8s-scale-app-rs
spec:
  schedule: "0 6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: scaler
              env:
                - name: K8S_SCALE_DEPLOYMENT
                  value: "my-app"
                - name: K8S_SCALE_REPLICAS
                  value: "3"
                - name: K8S_SCALE_DRY_RUN
                  value: "false"
                - name: RUST_LOG
                  value: "info"
