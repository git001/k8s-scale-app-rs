apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: k8s-scale-app-rs-scaler
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get"]
  - apiGroups: ["apps"]
    resources: ["deployments/scale"]
    verbs: ["get", "patch", "update"]
