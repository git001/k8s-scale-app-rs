apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Optional component: include from an overlay to create the namespace-scoped
# Role + RoleBinding that grant the "deploy" ServiceAccount permission to
# read deployments and patch the deployments/scale subresource.

resources:
  - role.yaml
  - rolebinding.yaml
